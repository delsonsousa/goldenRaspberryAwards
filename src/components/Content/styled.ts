import styled from "styled-components";

export const Content = styled.main`
  display: flex;
  gap: 16px;
  flex-wrap: wrap;
  justify-content: center;
  flex-grow: 1;
  padding: 32px;
  > div.list {
    width: 100%;
  }
`;
